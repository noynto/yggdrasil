apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: ntfy-tls
  namespace: ntfy
spec:
  secretName: ntfy-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  commonName: ntfy.noynto.me
  dnsNames:
    - ntfy.noynto.me